struct Stats {
    occupancy: bool,
    // 1 and 2 for players 1 and 2 respectively, and 0 for no piece on the space.
    team: i8,
}

fn main() {
    println!("Hello, world!");
    let mut v = Vec::new();
}

// for structs, use 1_2 as in (1, 2).